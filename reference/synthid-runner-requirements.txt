# Supported cross-platform runner/oracle roots. The release gate uses the full
# hash-pinned Linux CPU closure in synthid-runner-linux-cpu.lock.
immutabledict==4.2.0
jax==0.11.0
jaxlib==0.11.0
safetensors==0.8.0
torch==2.13.0
transformers==5.15.0
